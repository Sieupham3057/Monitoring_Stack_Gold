FRONTEND_PORT=8080
VITE_APP_NAME=Shop Admin
VITE_REQUEST_TIMEOUT_MS=30000

# Backend chạy trực tiếp trên máy host:
API_UPSTREAM=http://host.docker.internal:5065

# Nếu backend cũng chạy trong cùng Docker Compose, dùng tên service:
# API_UPSTREAM=http://backend:8080
